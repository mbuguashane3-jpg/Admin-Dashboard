{
  "type": "module",
  "scripts": {
    "postinstall": "npm install --prefix . && npm install --prefix backend",
    "build": "cd backend && npm run build",
    "dev": "npm run dev --prefix backend",
    "start": "npm run start --prefix backend"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
