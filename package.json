{
  "type": "module",
  "scripts": {
    "build": "npm install --prefix backend && cd backend && npm run build",
    "dev": "npm run dev --prefix backend",
    "start": "cd backend && node dist/index.js"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}